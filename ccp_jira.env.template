# Jira credentials for jira-pr-lookup skill
# Copy this file to ccp_jira.env and fill in your credentials
# NEVER commit ccp_jira.env to git (add it to .gitignore)

JIRA_BASE_URL=https://ccp.sys.comcast.net
JIRA_API_VERSION=2
JIRA_USER=svc-autotriage
JIRA_TOKEN=<your-jira-token>
