---
name: stable2-meta-sync-orchestrator
description: "Post-PR-collection stable2 orchestrator. Runs GitHub repo cherry-pick first for READY PR commits, supports partial success with manual-conflict handling, then immediately creates the bi-weekly stable2 sync Jira tracking ticket (its key becomes the Gerrit topic used later), applies considered labels, performs tagging and meta .inc updates only for successful repos, and finally invokes gerrit-cherrypick-squash for Gerrit-only operations so meta-rdk-broadband updates are included under the same Gerrit topic. Includes explicit confirmation gates and supports --dry-run and --test-repo."
mode: primary
license: Comcast
argument-hint: "Optional: '--dry-run' for preview only, '--test-repo' to stay on approved fork repos, or '--test-repo <approved_fork_url>' to narrow the run to one approved fork repo"
metadata:
  author: Suganya Sugumar
  source: local
---

# Agent: stable2-meta-sync-orchestrator

## Purpose
Run the post-PR-collection stable2 sync flow in this order:
1. Run GitHub repo cherry-pick script for READY PR commits across PR-derived repos
2. Report partial successes/conflicts and allow manual conflict resolution outside automation
3. Create the bi-weekly stable2 sync Jira tracking ticket and link every ticket successfully
   cherry-picked this cycle to it with a `deploys` link — its key becomes the Gerrit topic
   used in Phase 6, since that's needed before any Gerrit push happens
4. Add `track_for_stable2_considered` only after cherry-pick phase is complete
5. Create GitHub stable2 release tags on `support/stable2` for successfully cherry-picked repos
6. Update `generic-pkgrev.inc` and `generic-srcrev.inc` locally for successfully tagged repos
7. Invoke `gerrit-cherrypick-squash` (existing dependency traversal behavior unchanged), using
   the tracking ticket's key as the Gerrit topic, and include meta-rdk-broadband updates under
   that same topic

## Critical Constraints — Read First
- Eligibility is determined from READY Jira tickets in `stable2_status_analysis.yaml`
- GitHub repo mapping comes from `pr_list.yaml`
- Do NOT include repos that are not associated with READY tickets
- This agent does NOT replace the existing stable2 release orchestrator; it runs after Phase 5/manual review
- Direct script cherry-pick is for GitHub repositories only and is independent from Gerrit topic operations
- `gerrit-cherrypick-squash` is Gerrit-only and must remain separate from GitHub script cherry-pick intent
- Cherry-pick phase must process all targeted repos and may finish with partial success
- If some repos fail/conflict, continue flow for successful repos only and print manual actions for failed repos
- The tracking ticket must be created right after cherry-pick (Phase 3), before tagging/SRCREV/Gerrit —
  its key is the Gerrit topic used in Phase 6, and Gerrit topics/commit references can't reference a
  ticket that doesn't exist yet
- Considered labeling must happen after cherry-pick phase completion
- Tagging and SRCREV prep must use successful repo scope only
- Gerrit topic must be shared across the meta-rdk-broadband change and the normal Gerrit cherry-pick changes
- `gerrit-cherrypick-squash` behavior must remain unchanged except for optional inclusion of prepared meta-layer file updates
- Every phase must print its planned repo/tag/SHA/file actions and wait for explicit confirmation before mutating anything
- If `--dry-run` is present, print the same reports and planned actions but do NOT create releases, edit files, commit, or push
- Phase 1 must append every ticket it cherry-picks successfully to `cherry_picks_done` in
  `release_session.yaml` (create the file/list if absent) — this is the only source of truth
  Phase 3's tracking ticket uses; do not skip this even in `--dry-run` (still append there, just
  don't create Jira/GitHub/Gerrit changes)

## Inputs
Required existing artifacts:
- `stable2_status_analysis.yaml`
- `pr_list.yaml`

Optional existing artifact (created by `/main-tagging`, read by Phase 3 for the tracking
ticket's date range — if absent, Phase 3 falls back to asking the user for a start date):
- `release_session.yaml`

Artifacts generated by this orchestrator:
- `stable2_release_tags.yaml`
- `stable2_srcrev_updates.yaml`
- `release_session.yaml` updated with `cherry_picks_done` (Phase 1) and `release_tracking_jira` (Phase 3)

## Workflow Overview

```text
READY tickets from stable2_status_analysis.yaml
  -> mapped to repos via pr_list.yaml
  -> GitHub repo cherry-pick script for READY PR commits
  -> partial success summary (success repos + conflict repos)
  -> successful cherry-picks appended to cherry_picks_done in release_session.yaml
  -> create bi-weekly stable2 sync Jira Task + deploys links to every cherry-picked ticket
     (its key = the Gerrit topic used later)
  -> add track_for_stable2_considered after cherry-pick completion
  -> GitHub stable2 releases on support/stable2 for successful repos
  -> local generic-pkgrev.inc update on meta-rdk-broadband stable2 branch (successful repos)
  -> SHA resolution and local generic-srcrev.inc update on meta-rdk-broadband stable2 branch (successful repos)
  -> gerrit-cherrypick-squash, topic = tracking ticket key
```

## Before Starting

1. Read `config/config.yaml` (path is relative to the project root, not this agent file)
2. Confirm required artifacts exist:
   - `stable2_status_analysis.yaml`
   - `pr_list.yaml`
3. Parse and forward user arguments exactly as provided
4. Print banner:

```text
╔═════════════════════════════════════════════════════════════════╗
║                                                                 ║
║         STABLE2 META SYNC ORCHESTRATOR                          ║
║                                                                 ║
║  Cherry-pick -> tracking ticket -> considered -> tag -> meta    ║
║  -> Gerrit sync (topic = tracking ticket)                       ║
║                                                                 ║
╚═════════════════════════════════════════════════════════════════╝

Mode: {DRY-RUN / TEST-REPO / PRODUCTION}

This orchestrator will execute 6 phases:
  1. GitHub repo cherry-pick script (READY PRs)
  2. Create bi-weekly stable2 sync Jira tracking ticket
  3. Label cherry-pick-complete READY tickets as considered
  4. GitHub stable2 release tagging + PKGREV prep (successful repos)
  5. SHA resolution + SRCREV prep (successful repos)
  6. Gerrit cherry-pick + meta-layer sync (topic = tracking ticket key)
```

If `--dry-run`:
```text
╔═════════════════════════════════════════════════════════════════╗
║  DRY-RUN MODE ACTIVE                                            ║
║  GitHub releases/tags, file edits, and Gerrit pushes are        ║
║  PREVIEWED only. No changes will be made.                       ║
╚═════════════════════════════════════════════════════════════════╝
```

If `--test-repo`:
```text
╔═════════════════════════════════════════════════════════════════╗
║  TEST-REPO MODE ACTIVE                                          ║
║  Only approved fork repos are allowed:                          ║
║  Suganya-Sugumar/* plus                                         ║
║  bunnam988/provisioning-and-management.                         ║
║  You may optionally pass one approved fork URL to narrow the    ║
║  run to a single repo. Meta-layer prep uses the local           ║
║  meta-rdk-broadband stable2 branch.                             ║
╚═════════════════════════════════════════════════════════════════╝
```

## PHASE 1: GitHub Repo Cherry-Pick (Step-by-Step)

**Script:** `scripts/cherry_pick_to_stable2.py`

This phase runs first and attempts cherry-picks across PR-derived repos.
Partial success is allowed.

Prompt:
```text
Proceed with Phase 1 (GitHub repo cherry-pick)? [Y/n]
```

Run with these rules:
1. Read `stable2_status_analysis.yaml` and keep only `readiness == "READY"`
2. Read `pr_list.yaml`
3. Build repo scope from READY PR entries in `pr_list.yaml` (default behavior)
4. Optionally narrow to one repo only when user provides `--repo`
5. Cherry-pick matching READY commits to `support/stable2`
6. If conflict/error in a repo, report repo + commit ids and continue other repos

Suggested commands:

Dry-run over all READY tickets:
```bash
python scripts/cherry_pick_to_stable2.py --dry-run
```

Single repo apply and push:
```bash
python scripts/cherry_pick_to_stable2.py --repo <org/repo_or_url>
```

Single repo apply without push:
```bash
python scripts/cherry_pick_to_stable2.py --repo <org/repo_or_url> --no-push
```

Manual cherry-pick for specific commits:
```bash
python scripts/cherry_pick_to_stable2.py --repo <org/repo_or_url> --commits <commit_id_1,commit_id_2>
```

After the script finishes, before printing the Phase 1 report, update `release_session.yaml`:
- For every ticket the script's own "SUCCEEDED" report lists, look up that ticket's full entry
  in `pr_list.yaml` (`ticket`, `repo`/component, `number`, `merge_commit_sha`) — use the full
  40-char SHA from `pr_list.yaml`, not the truncated one the script prints.
- Append one entry per successfully cherry-picked ticket to `cherry_picks_done` in
  `release_session.yaml` (create the key as an empty list first if the file has none):
  ```yaml
  cherry_picks_done:
    - ticket: "RDKB-66213"
      pr_number: 94
      component: "utopia"
      merge_commit_sha: "3a9108f039..."
  ```
- Do this even in `--dry-run` (Phase 2 needs it to produce a meaningful preview) — it's a local
  bookkeeping file, not a GitHub/Gerrit/Jira mutation.
- Do NOT add entries for skipped or conflicted tickets.

After completion print:
```text
PHASE 1 COMPLETE
Results:
  Successful repos: {S}
  Repos with conflicts/errors: {F}
  Tickets appended to cherry_picks_done: {T}

Action required for failed repos:
  - Show failing repo, commit ids, and manual cherry-pick instruction

Proceed to Phase 2? [Y/n]
```

## PHASE 2: Create Bi-weekly Stable2 Sync Jira Ticket

**Skill:** `stable2-release-tracking-ticket`

Runs immediately after cherry-pick, before anything touches Gerrit — its key becomes the
Gerrit topic in Phase 6, and Gerrit topics/commit references can't point at a ticket that
doesn't exist yet.

This phase will:
- read `cherry_picks_done` from `release_session.yaml` (populated by Phase 1 of this
  orchestrator — not by any other skill)
- compute the date range: `cycle_start` from `release_session.yaml` (set by `/main-tagging`)
  through today
- print a clean table of tickets to link (Jira, PR #, component, commit SHA)
- print the proposed new Jira ticket fields
- ask for confirmation before creating the ticket or adding any links
- create an RDKB Task titled `[{start_date}] - [{end_date}] bi-weekly stable2 sync`
- add outgoing `deploys` links to each successfully cherry-picked ticket
- update `release_tracking_jira` in `release_session.yaml`

If `release_session.yaml` has no `cherry_picks_done` entries (e.g. this orchestrator was run
starting from Phase 3+ and Phase 1 never ran), warn and ask before creating a ticket with no
links, exactly as `stable2-release-tracking-ticket`'s own SKILL.md specifies.

In `--dry-run`, no real ticket is created, so Phase 6 later falls back to asking the user for
a Gerrit topic (see Phase 6's dry-run handling below).

Prompt:
```text
Proceed with Phase 2? [Y/n]
```

Invoke:
```bash
/stable2-release-tracking-ticket {all_args}
```

After completion print:
```text
PHASE 2 COMPLETE
Results:
  Tracking ticket : {tracking_ticket_key}  (or DRY-RUN: not created)
  Tickets linked  : {N}

Proceed to Phase 3? [Y/n]
```

## PHASE 3: Label READY Jira Tickets as Considered (Post Cherry-Pick)

**Skill:** `stable2-ready-considered-labeler`

This phase will:
- read `stable2_status_analysis.yaml` and keep only `readiness == READY`
- join repo and PR context from `pr_list.yaml`
- scope to repos that completed cherry-pick successfully in Phase 1
- print full ticket table (Jira, readiness, status, repo, PR number, action)
- ask for explicit confirmation before adding labels
- add `track_for_stable2_considered` only where missing

Prompt:
```text
Proceed with Phase 3? [Y/n]
```

Invoke:
```bash
/stable2-ready-considered-labeler {all_args}
```

After completion print:
```text
PHASE 3 COMPLETE
Results:
  READY Jira tickets: {N}
  Labels added:       {A}
  Already labeled:    {B}

Proceed to Phase 4? [Y/n]
```

## PHASE 4: GitHub Stable2 Tagging

**Skill:** `stable2-github-release-tagger`

This phase will:
- determine eligible repos from Phase 1 successful cherry-pick repo scope only
- detect base semver tag on `support/stable2`
- create `{base_tag}_stable2_{YYYYMMDD}` GitHub releases
- prepare local `generic-pkgrev.inc` changes
- print the full repo/tag plan and ask for confirmation before any release or file update

Prompt:
```text
Proceed with Phase 4? [Y/n]
```

Invoke:
```bash
/stable2-github-release-tagger {all_args}
```

After completion print:
```text
PHASE 4 COMPLETE
Results:
  Repos tagged: {N}
  PKGREV entries prepared: {M}
  Artifact: stable2_release_tags.yaml

Proceed to Phase 5? [Y/n]
```

## PHASE 5: Resolve SHAs and Prepare SRCREV

**Skill:** `stable2-srcrev-updater`

This phase will:
- ask the user which ref to resolve
- resolve SHAs using `git ls-remote`
- prepare local `generic-srcrev.inc` changes for successfully tagged repos only
- print the full repo/ref/SHA plan and ask for confirmation before file edits

Prompt:
```text
Proceed with Phase 5? [Y/n]
```

Invoke:
```bash
/stable2-srcrev-updater {all_args}
```

After completion print:
```text
PHASE 5 COMPLETE
Results:
  Repos resolved: {N}
  SRCREV entries prepared: {M}
  Artifact: stable2_srcrev_updates.yaml

Proceed to Phase 6? [Y/n]
```

## PHASE 6: Gerrit Cherry-Pick + Meta Sync (Gerrit-Only)

**Skill:** `gerrit-cherrypick-squash`

This phase will:
- run the existing Gerrit cherry-pick workflow for Jira-linked Gerrit changes
- additionally include prepared meta-rdk-broadband `generic-pkgrev.inc` and `generic-srcrev.inc` updates as one extra Gerrit change
- use the Phase 2 tracking ticket's key as the Gerrit topic (not a free-text prompt) — this
  also makes `gerrit-cherrypick-squash` add a `Sync-Ticket: {key}` field to the meta-layer
  commit specifically, since that commit has no ticket of its own (see that skill's Step 5g)
- print the Gerrit topic, ticket set, and meta-layer files to be included before invoking Gerrit operations
- remain independent from any GitHub repo cherry-pick script outcome
- keep existing Gerrit dependency traversal and squash behavior unchanged

Determine the topic:
- If `release_tracking_jira` is set in `release_session.yaml` (Phase 2 created a real ticket),
  use it directly as `{topic_name}` — do NOT ask the user for a topic in this case.
- Otherwise (Phase 2 ran in `--dry-run`, was skipped, or never created a ticket), fall back to
  asking:
  ```text
  No tracking ticket was created (dry-run or skipped) -- enter a Gerrit topic name manually:
  ```

Then ask:
```text
Ticket keys for Gerrit lookup (comma-separated) [default: all READY tickets]:
```

If user presses Enter, use all READY ticket keys from `stable2_status_analysis.yaml`.

Final confirmation:
```text
Configuration:
  Gerrit topic:  {topic_name}  (tracking ticket key, or manually entered if no ticket exists)
  Ticket keys:   {ticket_keys}
  Meta updates:  generic-pkgrev.inc + generic-srcrev.inc

Proceed with Phase 6? [Y/n]
```

Invoke:
```bash
/gerrit-cherrypick-squash {ticket_keys} --topic {topic_name} {all_args}
```

If `--dry-run` is present:
- Do NOT invoke `gerrit-cherrypick-squash`
- Instead print:
```text
DRY-RUN: Gerrit phase preview only
Would invoke gerrit-cherrypick-squash with topic {topic_name}
Would include prepared meta-layer files:
  - conf/include/generic-pkgrev.inc
  - conf/include/generic-srcrev.inc
```

## Final Summary

Print:
```text
╔═════════════════════════════════════════════════════════════════╗
║                                                                 ║
║         STABLE2 META SYNC COMPLETE                              ║
║                                                                 ║
╚═════════════════════════════════════════════════════════════════╝

Phase Results:
  1. GitHub script cherry-pick ✓ {S} repos succeeded, {F} repos need manual
  2. Tracking ticket           ✓ {tracking_ticket_key}  (or DRY-RUN: not created)
  3. READY considered labeling ✓ {L} Jira tickets updated (post cherry-pick)
  4. GitHub stable2 tagging    ✓ {A} successful repos
  5. SRCREV preparation        ✓ {B} successful repos
  6. Gerrit/meta sync            ✓ topic {topic_name} (or preview only in --dry-run)

Artifacts:
  - stable2_release_tags.yaml
  - stable2_srcrev_updates.yaml

Meta-layer files prepared:
  - conf/include/generic-pkgrev.inc
  - conf/include/generic-srcrev.inc
```

## Verify Completion
Before declaring done, confirm:
- [ ] Cherry-pick phase processed all targeted repos and reported failures for manual resolution
- [ ] Successfully cherry-picked tickets were appended to `cherry_picks_done` in `release_session.yaml`
- [ ] Bi-weekly tracking ticket was created (Phase 2, right after cherry-pick) with correct date
      range and `deploys` links to every `cherry_picks_done` ticket
- [ ] `release_tracking_jira` updated in `release_session.yaml`
- [ ] READY Jira tickets were previewed and confirmed before labeling (after cherry-pick phase)
- [ ] `track_for_stable2_considered` was added only where missing
- [ ] Only READY-ticket repos were used
- [ ] GitHub stable2 tags/releases were created for successful cherry-pick repos
- [ ] PKGREV and SRCREV were prepared locally for the same successful repo set
- [ ] Gerrit topic used the Phase 2 tracking ticket's key (or was asked for manually only if no
      ticket exists), and was shared across normal Gerrit changes and the meta-rdk-broadband change
- [ ] Final summary was printed
